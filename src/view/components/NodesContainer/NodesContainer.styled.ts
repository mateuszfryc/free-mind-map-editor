import styled, { css } from 'styled-components';

export const NodesContainer = styled.div(
    () => css`
        height: 100%;
        position: absolute;
        width: 100%;
        z-index: 2;
    `
);
